services:
  app:
    image: ${IMAGE_NAME}
    container_name: ${IMAGE_NAME}
    ports:
      - "8382:8080"
    networks:
      - firefly_firefly_iii
    restart: unless-stopped

networks:
  firefly_firefly_iii:
    external: true